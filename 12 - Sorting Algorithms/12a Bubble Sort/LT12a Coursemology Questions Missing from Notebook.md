# LT12a Bubble Sort: Coursemology Questions Missing from Notebook

Source: https://yijc.coursemology.org/courses/3257/assessments/88731  
Extracted: 1 September 2026

## Why this file exists

The local notebook starts at Coursemology Question 6. This file preserves Questions 1 to 5 so a notes tool scanning this folder can see the complete LT coverage.

## Learning coverage added

- Number of passes in Bubble Sort
- Verbal description of the algorithm
- In-place sorting
- Stability of sorting algorithms
- Worst-case time complexity

## Q1: Number of passes needed

For the array `[6, 5, 4, 3, 2, 1]`, what is the maximum number of passes required to sort the array using the Bubble Sort algorithm?

Choices:

- 4
- 5
- 6
- 7

## Q2: Bubble sort algorithm

Describe a Bubble Sort algorithm to sort a sequence of size `n` in ascending order.

Use key words such as **pass**, **compare**, **repeat**, **adjacent** and **swap**. Describe the first three passes to aid your explanation.

## Q3: Concept of in-place sorting

Which statements are true about in-place sorting of an array?

- Bubble Sort does not require additional memory space for an additional array.
- At the end of Bubble Sort, a new sorted array is created.
- The original array is mutated during Bubble Sort.

## Q4: Concept of stability of a sorting algorithm

The original array `arr = [9, 4, 3, 9, 3, 1]` is unsorted. Several candidate sorted results are shown. Determine which result represents a **stable sort**, and consider these statements:

- Result 1 shows the result of a stable sort.
- Result 2 shows the result of a stable sort.
- Result 3 shows the result of a stable sort.
- Whether a sort is stable depends on the type of sort.
- Whether a sort is stable depends on the algorithm.

Note: Coursemology visually distinguishes the repeated `3` and `9` instances. That visual formatting is not preserved in plain-text extraction. The tested concept is whether equal-valued elements retain their original relative order.

## Q5: Time complexity

What is the worst-case time complexity for Bubble Sort?

Hint: Consider a case where the elements are arranged in reverse order.

Choices:

- `O(n)`
- `O(n²)`
- `O(log n)`
- `O(n log n)`

## Already represented in the notebook

Questions 6 to 11 cover swapping adjacent elements, completing one pass, simple Bubble Sort, improved Bubble Sort, optimised Bubble Sort and comparison counting.
